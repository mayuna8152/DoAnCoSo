@model IEnumerable<DoAnCoSo.Models.Comment>

@{
    ViewData["Title"] = "DanhMucComment";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";

}

<h1>DanhMucComment</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ChatData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ChatData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.IdPost)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCmt">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCmt">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCmt">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
